---
spring:
  application:
    name: ms-zipkin
  cloud:
    config:
      uri: http://localhost:6666
      failFast: true
---
# Info
info:
  app:
    name: @project.name@
    description: @project.description@
    version: @project.version@