---
spring:
  application:
    name: ms-admin
  cloud:
    config:
      uri: http://localhost:6666
      failFast: true
---
# Info
info:
  app:
    name: @project.name@
    description: @project.description@
    version: @project.version@